@model Reciclajejuego.AppMVC.Models.Residuo

@{
    ViewData["Title"] = "Editar Residuo";
}

<style>
    .main-container {
        min-height: 100vh;
    }

    .card-custom {
        background: #e1f5fe;
        border-radius: 30px;
        padding: 35px;
        max-width: 700px;
        width: 100%;
        margin: auto;
        box-shadow: 0 10px 30px rgba(0,0,0,0.2);
    }

    .titulo {
        background: linear-gradient(135deg, #667eea, #5b86e5);
        color: white;
        padding: 12px;
        border-radius: 50px;
        text-align: center;
        font-weight: bold;
        margin-bottom: 25px;
        font-size: 1.5rem;
    }

    .form-control {
        border-radius: 20px;
        padding: 10px;
    }

        .form-control:focus {
            border-color: #5b86e5;
            box-shadow: 0 0 5px rgba(91,134,229,0.5);
        }

    .btn-guardar {
        background: linear-gradient(135deg, #43e97b, #38c172);
        color: white;
        border-radius: 25px;
        padding: 12px;
        width: 100%;
        border: none;
        font-weight: bold;
        margin-bottom: 10px;
        transition: 0.3s;
    }

        .btn-guardar:hover {
            transform: scale(1.03);
            color: white;
        }

    .btn-volver {
        background: linear-gradient(135deg, #90a4ae, #607d8b);
        color: white;
        border-radius: 25px;
        padding: 12px;
        width: 100%;
        text-align: center;
        display: inline-block;
        text-decoration: none;
        font-weight: bold;
        transition: 0.3s;
    }

        .btn-volver:hover {
            transform: scale(1.03);
            color: white;
        }
</style>

<div class="container mt-5 d-flex justify-content-center">

    <div class="contenedor text-center">

        <!-- 🔹 TÍTULO -->
        <div class="mb-3">
            <span class="titulo">✏️ Editar Residuo</span>
        </div>

        <form asp-action="Edit" enctype="multipart/form-data">

            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <input type="hidden" asp-for="Id" />
            <input type="hidden" asp-for="ImagenUrl" />

            <!-- NOMBRE -->
            <div class="mb-3 text-start">
                <label asp-for="Nombre"></label>
                <input asp-for="Nombre" class="form-control" />
                <span asp-validation-for="Nombre" class="text-danger"></span>
            </div>

            <!-- DESCRIPCIÓN -->
            <div class="mb-3 text-start">
                <label asp-for="Descripcion"></label>
                <input asp-for="Descripcion" class="form-control" />
                <span asp-validation-for="Descripcion" class="text-danger"></span>
            </div>

            <!-- PUNTOS -->
            <div class="mb-3 text-start">
                <label asp-for="Puntos"></label>
                <input asp-for="Puntos" class="form-control" />
                <span asp-validation-for="Puntos" class="text-danger"></span>
            </div>

            <!-- CONTENEDOR -->
            <div class="mb-3 text-start">
                <label asp-for="ContenedorId"></label>
                <select asp-for="ContenedorId"
                        class="form-control"
                        asp-items="ViewBag.ContenedorId">
                </select>
                <span asp-validation-for="ContenedorId" class="text-danger"></span>
            </div>

            <!-- IMAGEN -->
            <div class="mb-3 text-start">
                <label>Nueva Imagen</label>
                <input type="file" name="archivoImagen" class="form-control" />

                @if (!string.IsNullOrEmpty(Model.ImagenUrl))
                {
                    <div class="text-center">
                        <img src="@Model.ImagenUrl" class="img-preview" />
                    </div>
                }
            </div>

            <!-- BOTONES -->
            <div class="d-flex justify-content-between mt-4">
                <a asp-action="Index" class="btn-volver">⬅ Volver</a>
                <button type="submit" class="btn-guardar">💾 Guardar</button>
            </div>

        </form>

    </div>

</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}